# 3 ways to use io.github.yonran.jna2pcsc.Smartcardio:

# 1. Modify <java_home>/jre/lib/security/java.security
#    TerminalFactory.getDefault()
# 2. JVM prop -Djava.security.properties=/path/to/override.java.security
#    TerminalFactory.getDefault()
# 3. Explicitly call Security.addProvider(new Smartcardio());
#    TerminalFactory.getInstance("PC/SC", null, Smartcardio.PROVIDER_NAME);

security.provider.9=io.github.yonran.jna2pcsc.Smartcardio